# 测试集分布统计与结论（基于 infer 诊断日志）

## 一、运行背景

- 日志时间：`2026-04-29 12:54:15 ~ 12:55:01`
- 推理模型：`global_step3624.layer=2.head=4.hidden=64.best_model/model.pt`
- 测试集样本数（dataset 报告）：`310,000`
- 序列截断长度：
  - `seq_a: 256`
  - `seq_b: 256`
  - `seq_c: 512`
  - `seq_d: 512`

---

## 二、数据分布统计

## 1) 请求时间（request_ts）分布

- 有效时间戳：`310,000 / 310,000`（缺失 `0`）
- 时间范围：
  - 最小：`1774222835`（`2026-03-22 23:40:35 UTC`）
  - 最大：`1774228424`（`2026-03-23 01:13:44 UTC`）
  - 跨度：`0.06 天`（约 `1.55 小时`）
- 分位数：
  - p01：`1774222889`
  - p50：`1774225617`
  - p99：`1774228362`

### 派生时间特征

- `hour_of_day`（3个取值）：
  - `0`: `202,810`（`65.42%`）
  - `1`: `42,368`（`13.67%`）
  - `23`: `64,822`（`20.91%`）
- `day_of_week`（2个取值）：
  - `0(Mon)`: `245,178`（`79.09%`）
  - `6(Sun)`: `64,822`（`20.91%`）
- `day_of_month`（2个取值）：
  - `21`: `64,822`（`20.91%`）
  - `22`: `245,178`（`79.09%`）
- `month_of_year`（1个取值）：
  - `2(M3)`: `310,000`（`100.00%`）
- `is_weekend`：
  - `weekday`: `245,178`（`79.09%`）
  - `weekend`: `64,822`（`20.91%`）
- `time_of_day_bucket`：
  - `0(00-05 夜间)`: `245,178`（`79.09%`）
  - `3(18-23 晚间)`: `64,822`（`20.91%`）

---

## 2) 各序列域长度分布

- `seq_a`：mean=`222.3`，p50=`256`，p95=`256`，max=`256`，空序列占比=`0.000`
- `seq_b`：mean=`203.2`，p50=`256`，p95=`256`，max=`256`，空序列占比=`0.001`
- `seq_c`：mean=`356.1`，p50=`386`，p95=`512`，max=`512`，空序列占比=`0.000`
- `seq_d`：mean=`494.7`，p50=`512`，p95=`512`，max=`512`，空序列占比=`0.001`

---

## 3) 序列新鲜度估计（由 time_bucket 反推上界）

- `seq_a`：p50=`54,000s(0.6d)`，p95=`604,800s(7.0d)`，p99=`1,641,600s(19.0d)`，max=`365d`
- `seq_b`：p50=`54,000s(0.6d)`，p95=`1,641,600s(19.0d)`，p99=`6,048,000s(70.0d)`，max=`365d`
- `seq_c`：p50=`172,800s(2.0d)`，p95=`1,123,200s(13.0d)`，p99=`4,320,000s(50.0d)`，max=`365d`
- `seq_d`：p50=`3,000s(约0.03d)`，p95=`86,400s(1.0d)`，p99=`604,800s(7.0d)`，max=`365d`

---

## 4) 各域 time_bucket 分布（采样统计）

- `seq_a`：n=`2,000,000`，mean=`56.35`，p10=`50`，p50=`57`，p90=`61`，p99=`62`，max=`63`
- `seq_b`：n=`2,000,000`，mean=`55.79`，p10=`49`，p50=`56`，p90=`62`，p99=`62`，max=`63`
- `seq_c`：n=`2,000,000`，mean=`61.04`，p10=`57`，p50=`63`，p90=`63`，p99=`63`，max=`63`
- `seq_d`：n=`2,000,000`，mean=`50.58`，p10=`45`，p50=`50`，p90=`55`，p99=`58`，max=`63`

---

## 三、核心结论（不仅是“分布”，而是“影响判断”）

1. **测试请求时间窗口极窄**  
   全量测试仅覆盖约 `1.55 小时`，远小于常规训练集的时间跨度。这意味着线上评估更像“特定时段切片”，不是全时段平均。

2. **绝对时间特征在测试集上低熵**  
   小时仅 `3` 个取值、星期仅 `2` 个取值、月份固定 `1` 个取值。  
   这类特征在该测试集上的区分能力弱，但在训练中可能参与了复杂交互，带来泛化不稳定风险。

3. **序列域存在明显“新鲜度异质性”**  
   `seq_d` 很新（中位仅约 `3000s`），`seq_c` 相对更旧（中位 `2d`）。  
   如果训练集各域新鲜度结构不同，`seq_c` 可能成为主要失配来源。

4. **序列长度普遍接近截断上限**  
   `seq_a/seq_b` 的 p50 已到 `256`，`seq_d` 的 p50/p95 到 `512`。  
   说明 truncation 行为对信息保留影响很大，近端行为与被截断尾部的取舍需要重点关注。

---

## 四、风险分级

- **高风险**：时间分布偏移（测试窗口过窄）导致时序相关特征泛化不稳  
- **中风险**：`seq_c` 新鲜度结构偏老，可能与训练分布不一致  
- **中风险**：高截断率带来的信息损失可能影响不同域贡献平衡  
- **待核查**：日志进度显示 `Processed 358,400`，高于 `310,000`，需确认是否重复读取或仅统计口径问题

---

## 五、可执行建议（按优先级）

1. **优先级 P0：先做一致性核查**  
   核对最终 `predictions` 条数与 `unique user_id` 数，确认是否存在重复样本推理。

2. **优先级 P1：训练采样向测试时段对齐**  
   对 `23/0/1 点 + Sun/Mon` 时段做重采样或加权，提升评估场景匹配度。

3. **优先级 P1：降低绝对时间编码的过拟合风险**  
   对绝对时间特征做约束（弱化权重/正则/替换为相对时间表达），减少“训练广时段、测试窄时段”冲突。

4. **优先级 P2：针对 `seq_c` 做 freshness 对齐**  
   在训练中加入 request_ts 扰动或新鲜度分层采样，让 `seq_c` 的时间差分布更接近测试。

5. **优先级 P2：复核截断策略**  
   结合特征重要性评估 `seq_a/b/c/d` 的 max_len 是否需要结构化调整，减少关键信号被截断。

---

## 六、一句话总结

本次测试集是一个**强时间切片场景**：请求时间高度集中、绝对时间特征低熵、序列新鲜度跨域差异明显。当前模型在该场景下可运行，但若要进一步稳住 AUC，重点应放在**时间分布对齐 + `seq_c` 新鲜度对齐 + 重复样本口径核查**。
